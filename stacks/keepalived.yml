version: '3.8'

services:
  keepalived:
    image: geoffh1977/keepalived:latest
    deploy:
      mode: global
      placement:
        constraints: [node.role == manager]
    environment:
      VIRTUAL_IP: "192.168.1.200"