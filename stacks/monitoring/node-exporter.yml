version: '3.8'
services:
  node-exporter:
    image: prom/node-exporter:v1.3.1
    command:
      - '--path.rootfs=/host'
    networks:
      - host
    volumes:
      - '/:/host:ro,rslave'
    deploy:
      mode: global
      
networks:
  host:
    external: true
